version: "3.3"
services:
  cortijo_socket:
    container_name: cortijo_socket
    restart: on-failure
    build: .
    ports:
    - "8200:8200"
    volumes:
    - /home/ubuntu/cortijo/deployment/config_url.yaml:/root/scr/config_url.yaml:ro
